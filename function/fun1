'''ques 1-write a program to receive three integers from keyboard and get their sum and product calculated through a user defined function
cal_sum_prod()'''

def cal_sum_prod(x,y,z):
    ss=x+y+z
    pp=x*y*z
    return ss ,pp
a=int(input('enter a:'))
b=int(input('enter b:'))
c=int(input('enter c:'))
s,p=cal_sum_prod(a,b,c)
print(s,p)
